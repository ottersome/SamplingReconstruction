from samprecon.agents.agent import Agent
from scipy.interpolate

class InterpolatingAgent(Agent):
    def __init__(self,strategies=['linear']):
        self.stategies = strategies
    def act(self, observation):
        if chose_strategy == 'linear';

